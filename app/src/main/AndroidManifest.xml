<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="autyzmsoft.pl.literowiec">

    <application
        android:name=".ZmienneGlobalne"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".DialogModalny"
            android:theme="@android:style/Theme.Dialog">
        </activity>

        <activity
            android:name=".UstawieniaActivity"
            android:label="@string/title_activity_ustawienia">
            <intent-filter>
                <action android:name="autyzmsoft.pl.literowiec.UstawieniaActivity" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

        <activity android:name=".ApkaInfo">
        </activity>


        <activity android:name=".InternalExternalKlasa">
        </activity>
        <activity
            android:name=".FileChooserActivity"
            android:label="@string/title_activity_file_choser">
        </activity>
        <activity android:name=".WersjaDemoOstrzez">
        </activity>
    </application>

</manifest>